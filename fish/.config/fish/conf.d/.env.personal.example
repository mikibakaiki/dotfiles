# Valid
# KEY=value
# KEY="value with spaces"
# export KEY=value

# Will be silently skipped (safe)
# # this is a comment
# KEY=                    ← empty value, skipped
# KEY=value=with=equals   ← only first = is used, fine


SOME_VARIABLE=some_value